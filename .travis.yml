sudo: required
language: bash
services: docker

env:
  - BUILD_PATH=0.13
  - BUILD_PATH=0.13/alpine
  - VERSION=0.13.0.0

script:
  - make build
  - make test

after_script:
  - docker images

before_deploy:
  - docker login -u "$REGISTRY_USER" -p "$REGISTRY_PASS"
  - make tags

deploy:
  provider: script
  script: make publish
  on:
    branch: master
